spring:
  application:
    name: restaurant
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/restaurant_test
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: create
server:
  port: 8083
